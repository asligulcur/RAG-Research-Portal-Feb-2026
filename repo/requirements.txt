# Phase 2 RAG System - Python Dependencies
# Only packages actually used in the code

# Core RAG Pipeline
numpy>=1.24.0,<2.0.0
sentence-transformers>=2.3.1
openai>=1.0.0
python-dotenv>=1.0.0

# PDF Processing (ingestion only)
PyPDF2>=3.0.1
pdfplumber>=0.11.0

# Utilities
tqdm>=4.66.0
requests>=2.31.0

# Phase 3: Web UI
streamlit>=1.28.0

# Artifact export (PDF)
fpdf2>=2.7.0

# Optional: FAISS (for make index and src/rag/retriever.py only)
# App/eval use numpy-based retrieval — no FAISS needed for those
# Uncomment if running: make index, or python src/rag/retriever.py
# faiss-cpu>=1.7.4
